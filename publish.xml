<?xml version="1.0"?>
<project name="Extranet0ffice-1.0" basedir="." default="build">
	
	<target name="prepare">
		<mkdir dir="${project.basedir}/builds"/>
	</target>
	
	<target name="build" depends="prepare">
		<move file="${project.basedir}/${xinc.project.name}-${xinc.build.label}.tar.gz" todir="${project.basedir}/builds"/>
		<move file="${project.basedir}/${xinc.project.name}-${xinc.build.label}.tar.bz2" todir="${project.basedir}/builds"/>
	</target>
	
</project>
